{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test-e2e-run-staging": "concurrently \"yarn workspace @nina/backend start:staging-test\" \"yarn workspace @nina/frontend test-e2e-run-staging\"",
    "build:all": "yarn workspace @nina/common build && yarn workspace @nina/typeorm build && yarn workspace @nina/backend build"
  },
  "devDependencies": {
    "concurrently": "4.1.0"
  }
}
